<%= form_for(@answer) do |f| %>
  <% if @answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer.errors.count, "error") %> prohibited this answer from being saved:</h2>

      <ul>
      <% @answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


 <h3><%= Question.find(@answer.question_id).content %></h3>

  <div class="field">
    <%= f.check_box :answer_a %>
    <%= Question.find(@answer.question_id).answer_a %>
    
  </div>
  <div class="field">
    <%= f.check_box :answer_b %>
    <%= Question.find(@answer.question_id).answer_b %>
  </div>
  <div class="field">
    <%= f.check_box :answer_c %>
    <%= Question.find(@answer.question_id).answer_c %>
  </div>
  <div class="field">
    <%= f.check_box :answer_d %>
    <%= Question.find(@answer.question_id).answer_d %>
  </div>
<br>
  <div class="actions">
    <button type="f.submit" class="btn btn-primary">Wyślij!</button>
  </div>
<% end %>

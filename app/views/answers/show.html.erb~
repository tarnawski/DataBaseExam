 <div class="row">
<div class="col-lg-4">
<h2>List of questions</h2>

<% @tab.each do |tab| %>
<% if (tab.answer_a==false) && (tab.answer_b==false) && (tab.answer_c==false) && (tab.answer_d==false) %>
<i class="fa fa-minus-square"></i>
<%else%> 
<i class="fa fa-check-square-o"></i>
<%end%>
<%= link_to Question.find(tab.question_id).content, answer_path(tab.id) %> <br>
<% end %>

<br> ID current question: 
<%= @answer.question_id%>

<br><br><br>
Progress: <%= ((@tab.length-@not_checked)*100)/@tab.length %>%
<div class="progress">
<div class="progress-bar progress-bar-success" style="width: <%= ((@tab.length-@not_checked)*100)/@tab.length %>%"></div>
</div> 
<br>
<%= link_to 'Finish test and send answers', answers_path, class: 'btn btn-warning' %>

</div>
<div class="col-lg-1">

</div>
<div class="col-lg-7">
<h2>Current question</h2>
<%= render 'form2' %>
</div>
</div> 

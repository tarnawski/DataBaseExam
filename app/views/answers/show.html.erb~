 <div class="row">
<div class="col-lg-2">
<h2>Questions</h2>

<% @tab.each do |tab| %>
<% if (tab.answer=='')%>
<i class="fa fa-minus-square"></i>
<%else%> 
<i class="fa fa-check-square-o"></i>
<%end%>
<%= link_to Question.find(tab.question_id).content, answer_path(tab.id) %> <br>
<% end %>


<br>
Progress: <%= ((@tab.length-@not_checked)*100)/@tab.length %>%
<div class="progress">
<div class="progress-bar progress-bar-success" style="width: <%= ((@tab.length-@not_checked)*100)/@tab.length %>%"></div>
</div> 
<br>
<%= link_to 'Finish test', answers_path, class: 'btn btn-danger' %>
<br><br><br>
<h4>You solve already:<br> 
<%=distance_of_time_in_words_to_now(session[:start])%></h4>

(Time is updated when you save ansver or <a href="javascript:location.reload()">refresh </a>page)
</div>
<div class="col-lg-1">

</div>
<div class="col-lg-9">
<h2>Current question (ID: <%= @answer.question_id%>)</h2>
<%= render 'form2' %>
<br>
<h3><%=@err%></h3>
<%- if @zmienna %>

<table class="table">
    <tr>
      <%  @zmienna.columns.each do |columns| %>
        <th><%= columns %> </th>
        <% end %>
    
     <%  @zmienna.rows.each do |rows| %>
     <tr>   
     <%  rows.each do |tom| %>
     
        <td><%= tom %> </td>
     <% end %>
	</tr>

     <% end %>
</tr>
<%end%>
</div>
</div> 

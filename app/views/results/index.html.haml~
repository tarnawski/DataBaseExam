- if notice
  %div.alert.alert-info
    %p#notice= notice

%h1 Lista wyników

%table.table
  %thead
    %tr
      %th Student
      %th Punkty
      %th Ocena
      %th
      %th
      %th

  %tbody
    - @results.each do |result|
      %tr
        %td= User.find(result.student_id).firstname+ ' ' + User.find(result.student_id).lastname
        %td= result.points
        %td= result.mark
        %td= result.created_at

        %td= link_to 'Pokaż', test_result_path(result.test_id, result, :student => result.student_id)
        %td= link_to 'Edytuj', edit_test_result_path(result.test_id, result)
        %td= link_to 'Usuń', test_result_path(result.test_id, result), :method => :delete, :data => { :confirm => 'Are you sure?' }
        
%br

= link_to 'Wróć', tests_path , class: 'btn btn-primary'

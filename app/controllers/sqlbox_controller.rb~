class SqlboxController < ApplicationController
  def index

Sqlbox.prepare_connection(1)

  @button = params[:button]
  if params[:sql]
  @sql = params[:sql]
  




  begin
    @zmienna2=Sqlbox.connection.exec_query (@sql)
  rescue 
    @err = "In Your's query is something wrong, so can't execute"
  end



end

  end
end
